import { StyledDropDown as s } from "components/shared/Styles/DropDown";
import styled from "styled-components"

export const StyledDropDown = styled(s)`
  position: absolute;
  top: calc(50% + 42px);
`